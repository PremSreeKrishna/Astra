# Implementation Prompt — LLM Intent Interpretation for Natural-Language Questions; As a Data Governance Administrator, I want LLM-generated reporting queries constrained by approved PostgreSQL rules so that sensitive automotive data is protected during AI-assisted reporting; As an Automotive Reporting Analyst, I want query results returned through an interactive interface with status feedback so that I can review PostgreSQL data efficiently and trust the reporting outcome (+23 more)

> Use this prompt with any AI-assisted development tool to implement this feature.
> Generated by Astra Spec-Driven Development.

## Context

You are implementing the **LLM Intent Interpretation for Natural-Language Questions; As a Data Governance Administrator, I want LLM-generated reporting queries constrained by approved PostgreSQL rules so that sensitive automotive data is protected during AI-assisted reporting; As an Automotive Reporting Analyst, I want query results returned through an interactive interface with status feedback so that I can review PostgreSQL data efficiently and trust the reporting outcome (+23 more)** feature.
Feature reference: **2072057114**.
Follow the specification exactly. Do not add features not described in the spec.

## Files to Reference

- **Project Context:** `specs/.devx/project-context.md`
- **Current State:** `specs/.devx/current-state.md`
- **Implementation Check:** `specs/.devx/implementation-check.md`
- **Feature Change Map:** `specs/.devx/change-maps/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more.md`
- **Golden Repo Guidelines:** `specs/.devx/guidance/golden-repo-guidelines.md` (REQUIRED when present)
- **Golden Repo Sources:** `specs/.devx/guidance/golden-repo-sources.md` (REQUIRED when present)
- **Golden UI Design System:** `specs/.devx/skills/ui-design/golden-ui-design-system.md` (REQUIRED when present)
- **Specification:** `specs/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more/specs.md`
- **Implementation Acceptance Checklist:** `specs/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more/requirements.md`


## Skill Files (Use In Your AI Prompt)

Use these skill files explicitly when implementing this feature:
- `specs/.devx/skills/implement/SKILL.md`
- `specs/.devx/skills/validate/SKILL.md`
- `specs/.devx/skills/next/SKILL.md`
- `specs/.devx/skills/autopilot/SKILL.md`
- `specs/.devx/skills/ui-design/SKILL.md`

## Work Items

- **[3385]** LLM Intent Interpretation for Natural-Language Questions
- **[3422]** As a Data Governance Administrator, I want LLM-generated reporting queries constrained by approved PostgreSQL rules so that sensitive automotive data is protected during AI-assisted reporting (5 pts)
  Acceptance:
1. Platform constrains LLM-driven query generation to approved PostgreSQL schemas, tables, views, and read-only operations and blocks unsafe or unauthorized statements before execution
2. Platform enforces role-based access controls and masks, redacts, or excludes sensitive automotive customer and vehicle fields in accordance with GDPR, CCPA, and internal data-governance policies
3. Platform records the natural-language prompt, interpreted intent, generated SQL, access decision, and execution outcome in an audit trail that supports traceability and compliance review
4. User can I want LLM-generated reporting queries constrained by approved PostgreSQL rules so that sensitive automotive data is protected during AI-assisted reporting through the documented happy-path flow
5. System validates required inputs before completing "I want LLM-generated reporting queries constrained by approved PostgreSQL rules so that sensitive automotive data is protected during AI-assisted reporting"
- **[3425]** As an Automotive Reporting Analyst, I want query results returned through an interactive interface with status feedback so that I can review PostgreSQL data efficiently and trust the reporting outcome (5 pts)
  Acceptance:
1. When an approved interpreted request is submitted, the platform retrieves data from PostgreSQL and renders the returned rows in an interactive results view without exposing raw database credentials, direct SQL editing controls, or unrestricted database access.
2. For at least 95% of successfully completed requests under normal operating conditions, the user receives either a processing status update or the final results within 10 seconds of submission.
3. The interactive interface displays the execution state for each request as queued, running, completed, or failed, and updates the visible state without requiring the user to resubmit the request.
4. Completed results include query context that ties the displayed output to the executed SQL and source PostgreSQL objects so the analyst can understand how the reporting outcome was produced.
5. If query execution fails or exceeds the normal response window, the interface shows a non-technical status or error message with the request identifier and does not display partial or misleading final results as complete.
- **[3434]** As a Data Governance Manager, I want AI-generated PostgreSQL queries validated against access and compliance rules so that automotive reporting data remains secure and governed (5 pts)
  Acceptance:
1. When POST /api/reporting/query-validations receives AI-generated SQL, the service shall reject any statement that is not read-only SELECT-based SQL, including INSERT, UPDATE, DELETE, ALTER, DROP, CREATE, COPY, and multi-statement payloads.
2. The validation service shall allow access only to PostgreSQL schemas, tables, and views mapped to the authenticated user's reporting role, and shall block execution when the SQL references an unapproved object.
3. If the generated SQL references protected customer, vehicle, financing, warranty, or dealer columns, the service shall apply the configured masking, redaction, or exclusion rule from policy metadata before the query is eligible for execution.
4. The service shall block queries that exceed configured safety controls, including missing or excessive row limits, disallowed join scope, or estimated execution timeout risk, and return a governed response without calling POST /api/reporting/query-executions.
5. Each validation attempt shall create an immutable audit record containing queryId, requester role, referenced objects, validation outcome, policy reason code, and timestamp so execution can be traced to a completed governance decision.
- **[3406]** As an Automotive Reporting Analyst, I want to ask natural-language questions about PostgreSQL reporting data so that I can obtain business insights without writing SQL (5 pts)
  Acceptance:
1. When an authenticated Automotive Reporting Analyst submits a non-empty natural-language question about approved automotive reporting domains, the platform creates an interpretation request and returns a response linked to a unique request identifier.
2. For a clear question referencing supported domains such as vehicle, warranty, dealer inventory, service history, or customer ownership, the platform produces structured intent data that includes the identified subject area and at least one requested metric, filter, grouping, or time element.
3. The platform maps recognized business terms in the analyst question to approved PostgreSQL schema context used for downstream processing and does not require the analyst to provide SQL syntax, table names, or column names.
4. If the submitted question is ambiguous, incomplete, or below the interpretation confidence threshold, the platform returns a clarification or processing status message that identifies the unresolved element instead of returning a successful interpretation.
5. If the request payload is invalid, exceeds configured input limits, or the interpretation service fails, the platform returns an error response with a non-success status, preserves the analyst session, and records the failure details for operational tracing.
6. When an authenticated Automotive Reporting Analyst submits a non-empty natural-language question for an approved automotive reporting domain, the system creates a query record and returns a candidate PostgreSQL SQL preview that uses only approved read-only schemas, tables, or views.
7. If a request contains ambiguous automotive terms or lacks enough context to resolve a valid relationship path, the system returns a clarification response that identifies the specific missing dimension, filter, or entity instead of finalizing SQL.
8. Generated SQL must be syntactically valid PostgreSQL and must not contain INSERT, UPDATE, DELETE, MERGE, DROP, ALTER, TRUNCATE, or references to objects outside the approved metadata catalog.
9. The interpretation response must include a queryId, interpretation status, mapped domain metadata, and either a SQL preview or a clarification payload so the client can retrieve the same artifact through GET /api/nl-queries/{queryId}.
10. Under normal operating conditions, the interpretation service must return a candidate SQL preview or clarification outcome within 5 seconds for approved requests and record latency and outcome in application logs.
- **[3442]** As a Reporting Analyst, I want to ask for automotive business data in natural language so that I can obtain insights without writing PostgreSQL queries (5 pts)
  Acceptance:
1. System converts a natural-language request about approved automotive entities into candidate read-only PostgreSQL SQL using approved schema metadata without requiring the Reporting Analyst to write SQL
2. System returns a clarification message instead of generating an executable query when the request is ambiguous, incomplete, or conflicts with approved business terminology
3. System records the original prompt, interpreted intent, and generated query context for traceability and compliance with internal audit requirements
4. adoWorkItemType
5. User can ask for automotive business data in natural language so that I can obtain insights without writing PostgreSQL queries through the documented happy-path flow
- **[3448]** As a Data Access Administrator, I want natural-language queries validated against role and data policies so that sensitive automotive data is not exposed through generated SQL (5 pts)
  Acceptance:
1. System validates each generated SQL statement against role-based access rules, approved PostgreSQL schemas, and read-only constraints before execution is permitted
2. System masks, redacts, or excludes restricted customer, financing, vehicle, and warranty fields based on column-level and row-level policy definitions applicable to the requesting role
3. System blocks SQL that violates security controls, contains unsafe patterns, or attempts policy bypass and records the denial with sufficient detail to support GDPR, CCPA, and audit review
4. User can I want natural-language queries validated against role and data policies so that sensitive automotive data is not exposed through generated SQL through the documented happy-path flow
5. System validates required inputs before completing "I want natural-language queries validated against role and data policies so that sensitive automotive data is not exposed through generated SQL"
- **[3445]** As an Operations Manager, I want interactive reporting results and status visibility so that I can obtain automotive business insights efficiently and trust the reporting outcome (5 pts)
  Acceptance:
1. System returns either interactive results or a processing status message for at least 95% of successful reporting requests within 10 seconds under normal operating conditions
2. System presents returned automotive business data with pagination, controlled row limits, and a visible link to the executed SQL so users can verify reporting explainability
3. System logs prompt content, generated SQL, execution outcome, timing, and access events in an auditable record that supports internal compliance and operational monitoring
4. User can I want interactive reporting results and status visibility so that I can obtain automotive business insights efficiently and trust the reporting outcome through the documented happy-path flow
5. System validates required inputs before completing "I want interactive reporting results and status visibility so that I can obtain automotive business insights efficiently and trust the reporting outcome"
- **[3451]** As an Automotive Operations Manager, I want validated natural-language queries to return interactive PostgreSQL results or status updates so that I can act on reporting requests quickly (5 pts)
  Acceptance:
1. When POST /api/nl-queries/execute is called with a queryId that is not in a validated state, the system rejects execution with an authorization-safe error response and does not submit SQL to PostgreSQL.
2. For validated queries, the system returns either the first page of formatted results or a status response containing queryId and execution state within 10 seconds for at least 95% of successful requests under normal operating conditions.
3. The system enforces configured row-limit, page-size, and execution-timeout rules on every PostgreSQL result retrieval, and no response returns rows beyond the permitted maximum for the request.
4. Each successful result response includes traceability metadata linking the returned rows to the executed SQL reference and the source dataset or data domain used to generate the output.
5. If execution exceeds the interactive threshold, GET /api/nl-queries/{queryId}/status returns a non-terminal status payload that the user can poll until results are available or the query is marked timed out or failed.
- **[3460]** As a Compliance Auditor, I want a complete audit trail for natural-language PostgreSQL interactions so that automotive reporting access can be reviewed and explained (3 pts)
  Acceptance:
1. System records an immutable audit trail for each natural-language PostgreSQL interaction including prompt, generated SQL, validation decision, execution outcome, user identity, timestamp, and accessed data domain
2. System allows only authorized audit roles to search and review query lifecycle records while protecting sensitive customer, vehicle, financing, and OEM-related information in accordance with internal confidentiality controls
3. System preserves audit evidence needed for regulatory and contractual review, including support for GDPR, CCPA, SOC 2, and automotive data-governance traceability requirements
4. User can I want a complete audit trail for natural-language PostgreSQL interactions so that automotive reporting access can be reviewed and explained through the documented happy-path flow
5. System validates required inputs before completing "I want a complete audit trail for natural-language PostgreSQL interactions so that automotive reporting access can be reviewed and explained"
- **[3463]** As a Reporting Analyst, I want to submit natural-language requests for vehicle, warranty, service history, and dealer reporting so that I can obtain SQL-backed insights without writing SQL manually (5 pts)
  Acceptance:
1. When an authenticated Reporting Analyst submits a non-empty natural-language request referencing approved automotive reporting entities, the system creates a query request record and returns a requestId with the interpreted request status.
2. For a request that can be confidently mapped to approved PostgreSQL reporting metadata, the system generates syntactically valid read-only SQL that uses only approved schemas, tables, or views.
3. If the request contains ambiguous entity references, unclear metrics, unsupported reporting topics, or incomplete time filters, the system does not return candidate SQL and instead returns a clarification or status message describing the blocking issue.
4. The system stores and exposes traceability details for each request, including the original natural-language text, interpreted intent summary, mapped schema context, generated SQL or clarification outcome, and requesting user identity.
5. The request submission and interpretation flow returns either generated output context or a processing status response within 10 seconds for normal successful requests.
- **[3472]** As a Security Administrator, I want generated SQL for customer, vehicle, warranty, and supplier reporting to be validated against access and compliance policies so that unauthorized or unsafe queries are blocked before execution (5 pts)
  Acceptance:
1. System validates generated SQL against approved PostgreSQL schemas, read-only rules, unsafe pattern detection, and role-based row and column restrictions before any execution occurs
2. System masks, redacts, or excludes sensitive automotive and personal data fields according to user role and data classification requirements aligned to GDPR, CCPA, and internal governance policies
3. System blocks unauthorized or non-compliant SQL execution and records the request, generated SQL, policy decision, and rejection reason in immutable audit logs
4. User can I want generated SQL for customer, vehicle, warranty, and supplier reporting to be validated against access and compliance policies so that unauthorized or unsafe queries are blocked before execution through the documented happy-path flow
5. System validates required inputs before completing "I want generated SQL for customer, vehicle, warranty, and supplier reporting to be validated against access and compliance policies so that unauthorized or unsafe queries are blocked before execution"
- **[3475]** As a Reporting Operations Manager, I want validated SQL requests to execute with status messaging, resource controls, and full audit logging so that automotive reporting remains reliable, traceable, and within service expectations (5 pts)
  Acceptance:
1. POST /api/reporting/query-requests/execute rejects requests unless the referenced SQL has already passed validation and the caller is authorized for the targeted PostgreSQL reporting source.
2. The execution service enforces configured page size, maximum row limit, timeout threshold, and execution resource controls on every approved request before sending SQL to PostgreSQL.
3. When execution completes within the interactive threshold, GET /api/reporting/query-requests/{requestId}/results returns the result payload and metadata correlated to the executed requestId.
4. When execution does not complete within the interactive threshold, GET /api/reporting/query-requests/{requestId}/status returns a non-empty processing state with the same requestId rather than leaving the request without feedback.
5. Each executed request writes correlated audit and observability records containing prompt reference, SQL text or SQL hash, execution outcome, latency, access identity, data source identifier, and timestamped lifecycle events.
- **[3484]** As a Reporting Analyst, I want the platform to generate SQL for approved reporting requests so that PostgreSQL database interaction uses a consistent query language (5 pts)
  Acceptance:
1. System generates SQL as the query language for approved PostgreSQL database interaction requests using approved automotive reporting schema metadata
2. System limits generated statements to read-only SQL patterns by default and does not produce write or schema-changing commands unless explicitly approved by policy
3. System returns a clarification or status message instead of executing SQL when the reporting request is ambiguous, incomplete, or cannot be safely mapped to approved schema objects
4. User can I want the platform to generate SQL for approved reporting requests so that PostgreSQL database interaction uses a consistent query language through the documented happy-path flow
5. System validates required inputs before completing "I want the platform to generate SQL for approved reporting requests so that PostgreSQL database interaction uses a consistent query language"
- **[3487]** As a Data Security Administrator, I want generated SQL validated against access and compliance rules so that automotive reporting data is protected before database interaction occurs (5 pts)
  Acceptance:
1. When POST /api/reporting/query/validate receives generated SQL with invalid PostgreSQL syntax, the system returns a blocked outcome and does not issue an execution approval token or status for downstream execution.
2. The validation service blocks SQL that references schemas, tables, views, or columns not permitted for the requester's role, including unauthorized access to customer, financing, registration, warranty, supplier, or vehicle-linked personal data fields.
3. If generated SQL contains a prohibited operation or join pattern that could bypass row-level access rules or combine datasets in a way that exposes restricted personal data, the system returns a policy-specific violation response identifying the blocking rule code.
4. For roles with partial access, the validation outcome reflects required masking, redaction, or exclusion handling for sensitive columns before any execution step can proceed, and unrestricted raw field access is denied when the role lacks permission.
5. Each validation request stores an immutable audit record containing validationId, requester identity, requester role, SQL fingerprint, policy version, decision outcome, blocking or approval reasons, and timestamp retrievable through GET /api/audit/query-validations/{validationId}.
- **[3495]** As an Operations Manager, I want approved SQL execution to be traceable and performance-controlled so that reporting workflows remain auditable and responsive (5 pts)
  Acceptance:
1. System executes only validated SQL against PostgreSQL and returns either query results or a processing status message within 10 seconds for at least 95% of successful requests under normal operating conditions
2. System applies pagination, row limits, timeout thresholds, and resource controls to every executed SQL statement to protect source databases and preserve reporting availability
3. System logs the full prompt-to-SQL-to-result lifecycle, including user access, generated SQL, execution outcome, and timing data, so audit and explainability requirements are met
4. User can I want approved SQL execution to be traceable and performance-controlled so that reporting workflows remain auditable and responsive through the documented happy-path flow
5. System validates required inputs before completing "I want approved SQL execution to be traceable and performance-controlled so that reporting workflows remain auditable and responsive"
- **[3498]** As a Reporting Analyst, I want to view retrieved automotive reporting data in an interactive grid so that I can review results without leaving the reporting platform (5 pts)
  Acceptance:
1. System displays approved retrieved datasets in an interactive grid with column headers, formatted values, and pagination controls for result sets that exceed a single page.
2. System shows the original reporting request context with the displayed results so the Reporting Analyst can understand what retrieved automotive data the interface represents.
3. System enforces configured row limits and responsive rendering behavior so large result sets remain usable without exceeding performance thresholds.
4. User can view retrieved automotive reporting data in an interactive grid so that I can review results without leaving the reporting platform through the documented happy-path flow
5. System validates required inputs before completing "view retrieved automotive reporting data in an interactive grid so that I can review results without leaving the reporting platform"
- **[3501]** As a Compliance Manager, I want sensitive automotive reporting results to be masked by role in the interactive interface so that customer, vehicle, and financial data stays protected (5 pts)
  Acceptance:
1. When an authenticated user opens GET /api/results/{requestId}, the response and interactive grid include only rows and columns permitted by the user's resolved reporting role, and restricted customer, vehicle, warranty, financing, or dealer fields are omitted before payload serialization.
2. For fields classified as maskable rather than fully suppressed, the interface displays the approved masked representation consistently across initial load, pagination, sorting, filtering, and manual refresh for the same requestId and security context.
3. If the authenticated user lacks permission to view the requested result set or its governing security context cannot be resolved, the system denies access with an authorization error and does not return raw result data or hidden field metadata to the client.
4. Each result-view interaction records an immutable audit event containing requestId, user identity, resolved role, applied row and column policy identifiers, timestamp, and display outcome such as rendered, partially masked, or denied.
5. Security enforcement for masking and access control completes before rendering and does not cause more than 5% of successful result-view requests to exceed 10 seconds under normal operating conditions.
- **[3510]** As a Reporting Analyst, I want the results interface to show processing status and support query refinement so that I can continue analysis when retrieved data is delayed or ambiguous (5 pts)
  Acceptance:
1. System displays a processing status message within the interactive response threshold when retrieved data cannot be shown immediately because execution is delayed, timed out, or still in progress.
2. System presents clear clarification or failure messages when a reporting request is ambiguous, unsafe to interpret, or too large to display interactively, without executing unsupported result retrieval behavior.
3. System allows the Reporting Analyst to submit a refined request from the results interface and preserves traceability between the original request, refined request, and resulting output.
4. User can I want the results interface to show processing status and support query refinement so that I can continue analysis when retrieved data is delayed or ambiguous through the documented happy-path flow
5. System validates required inputs before completing "I want the results interface to show processing status and support query refinement so that I can continue analysis when retrieved data is delayed or ambiguous"
- **[3513]** As a Reporting Analyst, I want to receive interactive results for an approved natural-language query so that I can analyze automotive reporting data without waiting on manual report creation (5 pts)
  Acceptance:
1. When an approved natural-language query completes successfully, the system returns an interactive result set to the authenticated Reporting Analyst within 10 seconds for at least 95% of normal-load requests.
2. Before displaying any result, the system validates the generated SQL against approved PostgreSQL schemas, enforces read-only execution, and applies configured row-level, column-level, and sensitive-field masking rules for automotive reporting data.
3. The interactive results view displays tabular data with column headers, sortable columns, pagination or scrolling support for multi-row datasets, and metadata that identifies the executed query request and source dataset lineage.
4. If a query cannot be safely executed because of schema violations, access-control restrictions, or SQL validation failures, the system blocks result display and returns a user-safe error message without exposing raw SQL or internal rule details.
5. The system records an audit trail for each returned result including requestId, authenticated user identity, approved reporting context, execution timestamp, executed SQL reference, dataset/source identifiers, and result delivery outcome.
- **[3522]** As a Reporting Analyst, I want to receive a processing status message when my natural-language query takes longer to complete so that I know the automotive report request is still being handled (3 pts)
  Acceptance:
1. System returns a user-facing processing status message within 10 seconds for successfully completed requests when interactive automotive reporting results are not yet ready within that threshold.
2. System shows a clear processing status without exposing restricted SQL statements, internal validation logic, or unauthorized customer, VIN, warranty, financing, or supplier data.
3. System logs the natural-language request, status-message response, timing data, and authorized reporting context so operational teams can monitor SLA attainment and investigate latency exceptions.
4. User can receive a processing status message when my natural-language query takes longer to complete so that I know the automotive report request is still being handled through the documented happy-path flow
5. System validates required inputs before completing "receive a processing status message when my natural-language query takes longer to complete so that I know the automotive report request is still being handled"
- **[3525]** As a Data Security Administrator, I want the platform to validate LLM-derived reporting queries against access and safety guardrails so that customer, vehicle, and OEM data is protected before execution (5 pts)
  Acceptance:
1. When POST /api/reporting/query/validate receives LLM-derived SQL, the platform permits execution only if the parsed statement is read-only and all referenced schemas, tables, and views exist in the approved PostgreSQL allowlist for the caller's role.
2. If generated SQL references a protected column classified for masking or exclusion, the platform either rewrites the projected field according to policy or rejects validation with a non-sensitive reason code when the caller lacks access.
3. The validation layer rejects SQL containing disallowed operations or unsafe patterns, including DDL, DML, unbounded wildcard retrieval from restricted objects, unauthorized joins, or predicates identified as policy-bypassing, and no execution request is sent to PostgreSQL for those cases.
4. The platform enforces query resource controls during validation by applying row-limit and timeout rules; queries missing required limits or exceeding configured thresholds are blocked with a status response indicating the specific control that failed.
5. Each validation attempt writes an immutable audit entry containing requestId, authenticated user identifier, prompt hash or reference, generated SQL text, policy checks evaluated, final allow-or-block decision, and blocking reason codes when applicable.
- **[3533]** As a Compliance Auditor, I want the platform to trace each natural-language reporting request from prompt through execution outcome so that AI-generated reporting remains explainable and reviewable (3 pts)
  Acceptance:
1. System logs each natural-language reporting request with the original prompt, interpreted intent, generated SQL, validation decision, execution outcome, and request timing in a linked audit record
2. System preserves immutable and role-restricted audit access so compliance reviewers can inspect reporting activity without violating GDPR, CCPA, or OEM confidentiality obligations
3. System tracks latency and outcome events for each request so operations teams can verify whether successful requests returned results or a processing status message within the required 10-second threshold
4. User can I want the platform to trace each natural-language reporting request from prompt through execution outcome so that AI-generated reporting remains explainable and reviewable through the documented happy-path flow
5. System validates required inputs before completing "I want the platform to trace each natural-language reporting request from prompt through execution outcome so that AI-generated reporting remains explainable and reviewable"
- **[3536]** As a Reporting Analyst, I want to run approved reporting queries against PostgreSQL so that I can retrieve automotive business data for analysis (5 pts)
  Acceptance:
1. When an authenticated Reporting Analyst submits an approved reporting query targeting authorized PostgreSQL reporting schemas, the platform executes it using read-only database access and returns a query identifier with the execution response.
2. If a PostgreSQL query completes within 10 seconds, the platform returns the result set in a paginated retrievable format that includes row data, page metadata, and total rows when available.
3. If a PostgreSQL query does not complete within 10 seconds but remains valid and running, the platform returns a processing status message and preserves the request for later retrieval by query identifier.
4. The platform rejects any query execution request that targets a non-approved PostgreSQL schema or attempts a non-read-only statement, and the response includes an authorization or validation error without running the query.
5. During PostgreSQL execution, the platform enforces configured row limits, pagination boundaries, and timeout thresholds so that oversized requests are truncated, paged, or terminated according to policy rather than running without limits.
- **[3544]** As a Reporting Analyst, I want to retrieve requested automotive reporting data from PostgreSQL so that I can analyze current vehicle, dealer, warranty, and service information without manual extraction (5 pts)
  Acceptance:
1. When a Reporting Analyst submits a valid request for vehicle, dealer, warranty, or service data, the system executes only against approved PostgreSQL schemas, tables, or views and returns the corresponding read-only result set.
2. If the requested dataset includes columns restricted for the Reporting Analyst role, the response applies configured masking or excludes those columns before results are returned.
3. The system rejects any generated or submitted query containing blocked operations such as INSERT, UPDATE, DELETE, DROP, COPY, or references to schemas outside the approved PostgreSQL allowlist.
4. The results endpoint returns paginated data with a queryId reference, enforced row caps, and source-aligned columns for the executed retrieval request.
5. Each successful or rejected retrieval request records an audit event containing the actor identity, target PostgreSQL source, validation outcome, and execution status.
- **[3553]** As a Data Governance Administrator, I want PostgreSQL retrieval requests and outcomes to be fully audited and performance-controlled so that automotive reporting access remains explainable, compliant, and operationally resilient (5 pts)
  Acceptance:
1. System records an immutable audit trail for each PostgreSQL retrieval request, including the request input, executed SQL, actor, timestamp, source reference, and execution outcome.
2. System returns either query results or a processing status message within 10 seconds for at least 95% of successful PostgreSQL retrieval requests under normal operating conditions.
3. System enforces pagination, row limits, and timeout thresholds on PostgreSQL retrieval workflows and captures failures or policy breaches for administrator review.
4. User can I want PostgreSQL retrieval requests and outcomes to be fully audited and performance-controlled so that automotive reporting access remains explainable, compliant, and operationally resilient through the documented happy-path flow
5. System validates required inputs before completing "I want PostgreSQL retrieval requests and outcomes to be fully audited and performance-controlled so that automotive reporting access remains explainable, compliant, and operationally resilient"



## Implementation Instructions

1. Read the local handoff files under `specs/.devx/`: project context, current state, implementation check, and this feature's change map
2. When Golden Repo guidance exists (under `specs/.devx/guidance/` or the ui-design golden files), you MUST read it before planning and reconcile each convention — apply it, or mark it not-applicable-to-this-stack with a one-line rationale — and include a short "Golden Repo Reconciliation" note in your output (verified local patterns override golden guidance on conflict)
3. Inspect existing code first using the change map; verify candidate files, routes, components, services, tests, and shared modules in the actual workspace
4. Create an implementation plan that prefers modifying existing code over creating parallel implementations
5. Read `specs/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more/specs.md` thoroughly before writing any code
6. Read this feature's Open Questions (`specs/.devx/features.json` `openQuestions[]`, or the `## Open Questions` section in `specs/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more/specs.md`). Interactive run: surface the consolidated list, ask the human user for a decision before coding, apply their answer, and record the decision plus a one-line rationale in `specs/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more/assumptions.md`. Unattended run: choose a reasonable assumption and record it plus a rationale in `specs/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more/assumptions.md` — never assume silently. A feature held at `needs-clarification` (tracker `NEEDS_CLARIFICATION`) has an unresolved blocking question and must be resolved and re-enabled before implementing
7. Implement each item from `specs/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more/requirements.md` one at a time
8. Validate each checklist item as you implement it

## Constraints

- Do not deviate from the specification
- Do not add features or behaviors not in the spec
- Follow the project's established patterns and conventions
- Existing code patterns override generic guidance when they conflict
- Do not rely on Astra or live Golden Repo access during IDE implementation
- Each implementation acceptance item in requirements.md must be satisfied

## Validation

After implementation, verify:
- [ ] Local handoff files were read before planning
- [ ] When Golden Repo artifacts exist, they were read and a "Golden Repo Reconciliation" note was produced (each convention Applied or Not-applicable-to-this-stack with rationale)
- [ ] Existing code was inspected before editing
- [ ] Open Questions were reviewed; each was surfaced/asked (interactive) or recorded with a decision + rationale in `specs/llm-intent-interpretation-for-natural-language-questions-as-a-data-governance-administrator-i-want-llm-generated-reporting-queries-constrained-by-approved-postgresql-rules-so-that-sensitive-automotive-data-is-protected-during-ai-assisted-reporting-as-an-automotive-reporting-analyst-i-want-query-results-returned-through-an-interactive-interface-with-status-feedback-so-that-i-can-review-postgresql-data-efficiently-and-trust-the-reporting-outcome-23-more/assumptions.md` (unattended), and no blocking Open Question was left unresolved and unrecorded (never assumed silently)
- [ ] All implementation acceptance items in `requirements.md` are satisfied
- [ ] All user scenarios from `specs.md` work correctly
- [ ] No extra features were added beyond the spec
- [ ] Code follows project conventions
